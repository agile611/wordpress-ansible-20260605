# tasks file for mysql
---
- name: install tools
  apt: name={{item}} state=present update_cache=yes
  with_items: "{{packages_database}}"
  notify: restart mysql

- name: install mysql-server
  apt: name=default-mysql-server state=present update_cache=yes

- name: ensure mysql listening on all ports
  lineinfile: dest=/etc/mysql/mariadb.conf.d/50-server.cnf regexp=^bind-address line="bind-address = {{mysql_bind_address}}"
  notify: restart mysql

- name: create demo database
  ansible.mysql.mysql_db: name={{db_name}} state=present

- name: create demo user
  ansible.mysql.mysql_user: name={{db_user}} password={{db_password}} priv={{db_name}}.*:ALL host='{{db_host}}' state=present

- name: elimnate /root/.my.cnf
  file: path=/root/.my.cnf state=absent